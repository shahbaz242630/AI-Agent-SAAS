# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

# environment & secrets — never commit real env files
.env
.env.local
.env.*.local
.env.production
.env.staging

# logs
*.log
logs/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# misc
.turbo/
.vercel/

# generated Prisma client (regenerated by `pnpm db:generate` / package build)
infrastructure/database/src/generated/

# internal business/project documents — LOCAL ONLY, never pushed
# (founder rule 2026-07-22: only code changes are committed)
Eva BRD v1.2 - Consolidated Build Specification.md
PHASE-0-HANDOFF.md
docs/

# founder's external-provider records (Supabase, Paddle, etc.) — never commit
detail/
details/
Details/
*.xlsx
*.xls
*.csv
~$*

# agent guidance + internal business/project documents — LOCAL ONLY
AGENTS.md

# agent MCP server wiring — LOCAL ONLY. Holds no secrets (tokens come from the
# OS environment), but it does name our Supabase/Trigger.dev project refs, and
# infrastructure identifiers stay out of the public repo (founder rule 2026-07-22).
.mcp.json

# NOTE: README.md and SECURITY.md stay tracked — they are GitHub-facing
# (repo landing page + vulnerability-reporting policy), not internal docs.

# Disposable local tooling and working files. The repo is PUBLIC, and this is
# where throwaway SQL, dumps and one-off scripts land — including things that
# query the cloud database. Nothing here is ever part of the product.
scratchpad/
