{
  "name": "@eva/worker",
  "version": "0.0.0",
  "private": true,
  "description": "Eva background jobs — Trigger.dev v4 task project (BRD 9.3, locked 2026-07-22)",
  "scripts": {
    "dev": "trigger dev",
    "deploy": "trigger deploy",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@eva/configuration": "workspace:*",
    "@sentry/node": "^10.67.0",
    "@trigger.dev/sdk": "^4.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@eva/testing": "workspace:*",
    "@types/node": "^22.15.0",
    "trigger.dev": "^4.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
